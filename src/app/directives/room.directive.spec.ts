import { RoomDirective } from './room.directive';

describe('RoomDirective', () => {
  it('should create an instance', () => {
    const directive = new RoomDirective();
    expect(directive).toBeTruthy();
  });
});
